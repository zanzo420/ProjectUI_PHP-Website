TO CHANGE root password:
1. type: mysqladmin -u root -p password
2. It will say "Enter password:" but root by default has no password!
3. press enter
4. It will say "New password:", type new password and type again after to confirm

mysqladmin password: Fireicexv83445!!

to change it again use:

mysqladmin -u root -p password (and press enter)

// go into cmd, and go to:
// C:\wamp64\bin\mysql\mysql5.7.9\bin
// then type: mysqladmin -u root password
// this will show New password:

// you can also type: mysql -u root -p
// this will open up mysql console like the normal WAMP does